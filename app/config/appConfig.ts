export const DatabaseConfig = {
    DATABASE_NAME: process.env.DATABASE_NAME || "robus_database",
    COLLECTION_NAME: process.env.COLLECTION_NAME || "robus_collection",
  };
  
// 如果将来需要添加更多配置，可以在这里继续添加
export const ApiConfig = {
// API 相关的配置
};

// 可以根据需要添加更多配置对象